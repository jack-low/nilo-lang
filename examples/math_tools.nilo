export func add(a: int, b: int) -> int {
    return a + b;
}

export func sum_to(n: int) -> int {
    let i = 0;
    let total = 0;
    while (i <= n) {
        total = total + i;
        i = i + 1;
    }
    return total;
}
